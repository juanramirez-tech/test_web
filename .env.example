# Copiar a `.env.local` (ese archivo no se versiona).
# AUTH_GATE debe ser el mismo valor AUTH del backend (.env).
# El proxy de ng serve lo envía en POST /login. Nunca lo pongas en src/environments.

API_PROXY_TARGET=http://localhost:3000
AUTH_GATE=
